# Declared by AutoResearch. Goals are milestones; the benchmark ref
# (suite/name/variant) is the same envelope the bench ledger records per
# measurement, so this file is what joins a milestone to its trajectory.
version: 1
benchmarks:
  [
    {
        suite: prove_phase_bench,
        name: blake3_m32,
        unit: hash/s,
        direction: higher,
        variants: [ throughput ]
      }
  ]
goals:
  [
    {
        title: 5M BLAKE3/s,
        benchmark: prove_phase_bench/blake3_m32/throughput,
        metric: throughput,
        target: 5000000,
        hardware: RTX 5090
      }
  ]
