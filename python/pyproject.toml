# Type-check config only — no [project] table, so no build backend ever reads it.
# The packaging manifest is the repo-root pyproject.toml, which points
# setuptools at this directory (`where = ["python"]`).
[tool.mypy]
files = ["flock_zorch"]
python_version = "3.11"
ignore_missing_imports = true
