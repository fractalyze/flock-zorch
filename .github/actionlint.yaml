# actionlint.yaml
#
# CI dispatches the GPU legs to self-hosted boxes by label; actionlint rejects
# any label it does not know unless they are declared here.

self-hosted-runner:
  labels:
    - cpu
    - gpu
