[build]
# Match flock's own bench build so the `bench_*_ligerito_cpu` baselines are
# flock's honest best on this hardware rather than an under-optimized build.
rustflags = ["-C", "target-cpu=native"]
